<% content_for :title, "新規登録" %>

<div class="page-head">
  <h1>本の新規登録</h1>
</div>

<div class="card">
  <%= render "form", book: @book %>
</div>

<%= link_to "← 一覧に戻る", books_path, class: "back-link" %>